generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_DEV_URL")
}

model User {
  id       String @id
  name     String
  username String
  age      Int
  email    String @unique
  password String

  @@map("users")
}
